# Postgres
POSTGRES_USER=spareparts
POSTGRES_PASSWORD=spareparts
POSTGRES_DB=spareparts
POSTGRES_PORT=5432

# Redis
REDIS_PORT=6379

# Elasticsearch (8.x requires memlock limits and JVM heap)
ELASTIC_PORT=9200
ELASTIC_PASSWORD=changeme
ELASTIC_HEAP=512m

DATABASE_URL=postgres://spareparts:spareparts@localhost:5432/spareparts

API_PORT=3000

# JWT
ACCESS_TOKEN_SECRET=change_me_in_prod
ACCESS_TOKEN_TTL_MINUTES=30
REFRESH_TOKEN_TTL_DAYS=30
